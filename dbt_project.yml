
name: 'feature_store'
version: '0.1.0'
config-version: 2

profile: 'user'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

require-dbt-version: [">=1.0.0", "<2.0.0"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
